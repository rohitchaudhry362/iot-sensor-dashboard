{
  "name": "simulator",
  "version": "1.0.0",
  "private": true,
  "description": "Publishes the sample sensor data to the MQTT broker as if it came from live devices.",
  "main": "dist/index.js",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^18.0.1",
    "joi": "^18.2.9",
    "mqtt": "^5.16.0",
    "winston": "^3.19.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3"
  }
}
